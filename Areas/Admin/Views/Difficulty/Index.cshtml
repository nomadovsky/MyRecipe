@model IEnumerable<Difficulty>
@{
    ViewData["Title"] = "Index";
}
<div class="container p-3">
        <div class="row pt-4">
            <div class="col-md-6">
                <h2>Difficulty</h2>
            </div>
            <div class="col-md-6 text-end">
            <a asp-controller="Difficulty" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i>
                Add Difficulty Level
            </a>
            </div>
        </div>
    </div>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>
                Difficulty Level
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td>
                    @obj.Name
                </td>
                <td width="20%">
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Difficulty" asp-action="Edit" asp-route-id="@obj.Id" 
                            class="btn btn-primary mx-2" 
                            style="border-top-right-radius: 5px; border-bottom-right-radius: 5px;">
                            <i class="bi bi-pencil"></i> Edit
                        </a>
                        <form asp-controller="Difficulty" asp-action="Delete" method="post" onclick="return confirm('Are you sure you want to delete this?')">
                            <input type="hidden" name="Id" value="@obj.Id" />
                            <button type="submit" class="btn btn-outline-danger mx-2"
                                data-confirm="Are you sure you want to delete this difficulty level?">
                                <i class="bi bi-x-lg"></i>
                                Delete
                            </button>
                        </form>
                        
                    </div>
                </td>
            </tr>
        }
        
    </tbody>
</table>